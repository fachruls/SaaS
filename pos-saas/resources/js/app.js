import './bootstrap';

// Note: Alpine.js is provided by Livewire 3 automatically.
// Do NOT import Alpine here again as it will cause double-initialization.
// If you need to extend Alpine with custom plugins or components,
// use the `alpine:init` Livewire event instead:
//
// document.addEventListener('alpine:init', () => {
//     Alpine.data('myComponent', () => ({ ... }));
// });
